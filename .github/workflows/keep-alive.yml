name: Keep Render awake

on:
  schedule:
    - cron: '*/5 * * * *'  

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://gathervia.onrender.com/api/v1/guests/health